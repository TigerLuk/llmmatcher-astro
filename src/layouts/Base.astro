---
// src/layouts/Base.astro
import '../styles/global.css';

export interface Props {
  title: string;
  description?: string;
  ogImage?: string;
  canonical?: string;
}
const {
  title,
  description = 'Find the best open-source LLM for your GPU �?instantly.',
  ogImage = '/og-default.png',
  canonical,
} = Astro.props;
const canonicalURL = canonical ?? new URL(Astro.url.pathname, Astro.site).href;
---
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="robots" content="index, follow, max-image-preview:large" />
  <meta name="description" content={description} />
  <meta name="theme-color" content="#0f1117" />
  <meta name="impact-site-verification" content="a5aaac23-3b38-438b-b453-e5d0d28e568a" />
  <link rel="canonical" href={canonicalURL} />

  <!-- Open Graph -->
  <meta property="og:type" content="website" />
  <meta property="og:title" content={title} />
  <meta property="og:description" content={description} />
  <meta property="og:url" content={canonicalURL} />
  <meta property="og:image" content={new URL(ogImage, Astro.site).href} />
  <meta property="og:site_name" content="LLM Matcher" />

  <!-- Twitter Card -->
  <meta name="twitter:card" content="summary_large_image" />
  <meta name="twitter:title" content={title} />
  <meta name="twitter:description" content={description} />
  <meta name="twitter:image" content={new URL(ogImage, Astro.site).href} />

  <title>{title}</title>

  <!-- Google Analytics -->
  <script async src="https://www.googletagmanager.com/gtag/js?id=G-SHF1GLMN0F"></script>
  <script is:inline>
    window.dataLayer = window.dataLayer || [];
    function gtag(){dataLayer.push(arguments);}
    gtag('js', new Date());
    gtag('config', 'G-SHF1GLMN0F');
    window.trackEvent = function(name, params) {
      gtag('event', name, params || {});
    };
  </script>

  <style>
    nav {
      border-bottom: 1px solid var(--border);
      padding: .8rem 0;
      position: sticky; top: 0;
      background: var(--bg);
      z-index: 100;
    }
    nav .inner {
      display: flex; align-items: center; justify-content: space-between;
      max-width: 1100px; margin: 0 auto; padding: 0 1.5rem;
    }
    .logo { font-weight: 700; font-size: 1.1rem; color: var(--text-primary); text-decoration: none; }
    .logo span { color: var(--accent); }
    .nav-links { display: flex; gap: 1.5rem; list-style: none; }
    .nav-links a { color: var(--text-secondary); font-size: .9rem; }
    .nav-links a:hover { color: var(--text-primary); text-decoration: none; }
    footer {
      border-top: 1px solid var(--border);
      margin-top: 4rem; padding: 2rem 0;
      color: var(--text-muted); font-size: .85rem; text-align: center;
    }
    footer a { color: var(--text-muted); }
    footer .footer-links { display: flex; justify-content: center; gap: 1.2rem; flex-wrap: wrap; margin-bottom: .5rem; }
    /* Prose styles for markdown-rendered pages */
    .prose h1 { font-size: 1.8rem; margin-bottom: 1rem; }
    .prose h2 { font-size: 1.2rem; margin-top: 2rem; margin-bottom: .6rem; border-bottom: 1px solid var(--border); padding-bottom: .4rem; }
    .prose p { margin-bottom: .8rem; color: var(--text-secondary); line-height: 1.75; }
    .prose ul { padding-left: 1.5rem; margin-bottom: .8rem; }
    .prose li { margin-bottom: .3rem; color: var(--text-secondary); line-height: 1.75; }
    .prose a { color: var(--accent); }
    .prose table { width: 100%; border-collapse: collapse; margin-bottom: 1rem; font-size: .9rem; }
    .prose th { background: var(--bg-hover); padding: .5rem .8rem; text-align: left; border: 1px solid var(--border); }
    .prose td { padding: .5rem .8rem; border: 1px solid var(--border); color: var(--text-secondary); }
    .prose code { background: var(--bg-hover); padding: .1rem .4rem; border-radius: 3px; font-size: .85em; }
    .prose em { color: var(--text-muted); }
    @media (max-width: 600px) { .nav-links { display: none; } }
  </style>
</head>
<body>
  <nav>
    <div class="inner">
      <a href="/" class="logo">⚡ LLM <span>Matcher</span></a>
      <ul class="nav-links">
        <li><a href="/">Matcher</a></li>
        <li><a href="/quiz">Quiz</a></li>
        <li><a href="/compare">Compare</a></li>
        <li><a href="/leaderboard">Leaderboard</a></li>
        <li><a href="/blog">Blog</a></li>
        <li><a href="/models">Models</a></li>
        <li><a href="/api/auth">Login</a></li>
      </ul>
    </div>
  </nav>

  <main>
    <slot />
  </main>

  <footer>
    <div class="container">
      <div class="footer-links">
        <a href="/about">About & Affiliate Disclosure</a>
        <a href="/privacy">Privacy Policy</a>
        <a href="/terms">Terms of Service</a>
        <a href="/blog">Blog</a>
      </div>
      <p>© 2026 LLM Matcher</p>
    </div>
  </footer>
</body>
</html>
